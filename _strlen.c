#include "main.h"

/**
 * _strlen - length of a string
 * @str: the string
 * Return: the length
 */

int _strlen(char *str)
{
	int index;

	for (index = 0; str[index] != '\0'; index++)
		;
	return (0);
}
